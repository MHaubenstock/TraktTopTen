# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'TraktTopTen' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  
  # Pods for TraktTopTen
  pod 'AsyncImageDownloader'
  pod 'SwiftyJSON'
  pod 'RateView'
  
  target 'TraktTopTenTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TraktTopTenUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
